import 'fullscreen_service.dart';

class WebFullscreen implements FullscreenService {
  static bool toggleFullScreen() => false;
  static bool isFullScreen() => false;
  static bool initialize() => true;
}
